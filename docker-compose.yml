
services:
  db:
    image: postgres:16
    container_name: lms_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: lms_dev
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d lms_dev || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10
    # (қалауыңыз бойынша) локалдан psql қосылу үшін:
    # ports:
    #   - "5433:5432"

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lms_web
    working_dir: /app
    depends_on:
      db:
        condition: service_healthy
    environment:
      MIX_ENV: "dev"
      PHX_SERVER: "true"
      PHX_HOST: "localhost"
      PORT: "4000"
      DB_HOST: "db"
      DB_USER: "postgres"
      DB_PASS: "postgres"
      DB_NAME: "lms_dev"
      DATABASE_URL: "ecto://postgres:postgres@db:5432/lms_dev"

      # МІНДЕТТІ: 8+ символ
      LIVE_VIEW_SIGNING_SALT: "dev_salt_please_change_123"
      # Ұзын keybase қою жақсы
      SECRET_KEY_BASE: "dev_secret_please_change_very_long_64_chars_min"
      
    ports:
      - "4001:4000"
    volumes:
      - ./:/app
      - deps:/app/deps
      - build:/app/_build
      - node_modules:/app/assets/node_modules
    command: >
      sh -lc "
        mix deps.get &&
        mix ecto.create &&
        mix ecto.migrate &&
        mix phx.server
      "

volumes:
  pgdata:
  deps:
  build:
  node_modules:
